<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">

    <Properties>
        <Property name="logPath">logs</Property>
        <Property name="logFileName">${ctx:browser}_test.log</Property>  <!--the ${ctx:browser} dynamically replaces the filename based on the browser name -->
    </Properties>

    <Appenders>
        <!-- Console Appender-->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%C] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- File Appender -->
        <RollingFile name="FileLogger" fileName="logs/Current_Test_Automation_dated_${date:yyyy-MM-dd}.log"
                     filePattern="logs/Previous_Test_Automation_dated_%d{yyyy-MM-dd}.log"> <!--defines the naming convention for the archived log files after rotation.-->
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/> <!-- interval of 1, meaning log files will be created every day -->
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- The Root logger: Logs info and above to both console and file -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
        </Root>
    </Loggers>
</Configuration>
